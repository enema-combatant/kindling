# Sample Documents

These files are embedded automatically when you start Mission 01.

**Replace them with your own.** Drop any `.txt` or `.md` files here and restart:

```bash
docker compose restart app
```

The sample files are designed to demonstrate semantic search — try searching
for concepts that don't appear as literal keywords in any document.
