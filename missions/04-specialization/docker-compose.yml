services:
  ollama:
    image: ollama/ollama:latest
    container_name: kindling-ollama-04
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kindling-specialist
    ports:
      - "5004:5004"
    env_file:
      - ../../.env
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      - CHROMA_PATH=/data/chromadb
      - DOMAIN_PATH=/app/domains/networking
    volumes:
      - chroma-data:/data/chromadb
    depends_on:
      ollama:
        condition: service_healthy

volumes:
  ollama-data:
  chroma-data:
